import { SalaryHighlightPipe } from './salary-highlight.pipe';

describe('SalaryHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new SalaryHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
